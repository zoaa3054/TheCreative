import styled from "styled-components";
import Spinner from "../Components/Spinner";

const StudentHomePage = ()=>{
    return(
        <Container>
            <h1>Hello student in home page</h1>
            
        </Container>
    );
};

export default StudentHomePage;

const Container = styled.div`
    
`;
